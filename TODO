Documention

- README description
- cerebro.1 manpage
- doc for writing modules
- cerebro api manpages

Testing
- fd leaking
- mem-leaking
- none module works if no clusterlist modules found

Cleanup
- reorg cerebro err codes
- naming convention, begin with _cerebro or not
- 80 columns
- still legit comments

Audit
- code review to reduce memory footprint
- maps confine only "cerebro_" instead of "cerebro*"
  - be more restrictive in general, don't just do cerebro*
  - or just specifically list the ones that should be exported

Portability
- IPv6
- Non-pthreads support

Design
- move listener hash into status hash 
- strerror_r and errormsg_r funcs
- libcerebro set configfile, module, etc. functions
- generic err message in cerebro_t handle, i.e for conffile errors
- config_is_setup and and equivalent functions
- pthreads locking for clusterlists/config modules?? (they are read only)

Before distribution
- do .h trick for header files like freeipmi

Debugging
- signals to daemon to log/unlog debug info?

GPL
- do it
- place header on all headers
- place on all c files
- note on manpage ORIGINS

Misc
- remove hash/list buffering in list.c hash.c?




