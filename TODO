- README description
  - cerebro.1 manpage
- IPv6
- Non-pthreads support?
- remove hash/list buffering in list.c hash.c?

- test
   - fd leaking
   - mem-leaking
   - none module works if no clusterlist modules found

- to GPL
  - on all headers
  - on manpage ORIGINS

- signals to daemon to log/unlog debug info?

- doc for writing modules

- maps confine only "cerebro_" instead of "cerebro*"
  - be more restrictive in general, don't just do cerebro*
  - or just specifically list the ones that should be exported

- strerror_r and errormsg_r funcs?
  - only if determined necessary at some point

- code review to reduce memory footprint

- reorg cerebro err codes

- consistency between ret vs. rv
  - err_out vs. cleanup
  - buf vs. buffer
  - buflen vs. bufferlen
  - error checking
    - x >= y
    - y < x
  - naming convention, begin with _cerebro or not
  - buflen checks -1 vs. +1 checks
  - NULL vs !

- set configfile, module, etc. functions
  - reload config, module, etc. functions

- pthreads locking for clusterlists/config modules?
  - probably not b/c both are read only

- .h file order consistency

- generic err message in cerebro_t handle?
  - i.e. for conffile

- instead of cerebro_defs.h cerebro_constants.h or something?

- do build trick for header files like freeipmi

- config_is_setup? and equiv funcs?